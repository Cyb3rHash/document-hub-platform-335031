# Interfaces

This folder contains interface artifacts used by the frontend to understand dependent services.

- `nextjs_backend_openapi.json`: OpenAPI specification for the backend service (downloaded from the running backend docs endpoint).
